# 豆仓 Pro 2.0

面向拼豆爱好者的网页版生产应用，覆盖账户、库存流水、图纸、逐格进度、补豆、智能制图和统计。

线上地址保持不变：<https://pingdou.onrender.com/>

## 核心能力

- 邮箱注册登录、账号隔离、密码重置、完整数据导出和账号注销
- MARD 221 色库存、批量出入库、CSV、盘点、撤销与审计流水
- 低库存预警、缺货计算、补豆克数和补豆口令
- 图纸上传、标签、文件夹、状态、消耗计算和一键出库
- CIELAB 色差匹配、可选抖动、颜色数限制和高清 PNG 导出
- 逐色及逐格拼制进度、只读分享链接
- 出入库趋势、色号消耗排名和匿名访问统计
- 手机、平板和桌面响应式页面

## 本地运行

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
Copy-Item .env.example .env
.\.venv\Scripts\python.exe app.py
```

打开 <http://127.0.0.1:5000/>。未设置 `DATABASE_URL` 时使用 `instance/pingdou.db`，仅适合本地开发。

## 测试

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
node --check static\app.js
```

## 正式环境变量

必须配置：

- `APP_ENV=production`
- `APP_SECRET`：高强度随机值
- `APP_URL=https://pingdou.onrender.com`
- `DATABASE_URL`：长期有效的 PostgreSQL 内部连接串
- `OWNER_EMAIL`：站点所有者邮箱
- `OWNER_PASSWORD`：站点所有者初始密码，至少 10 位
- `OWNER_NAME`：站点所有者显示名

密码重置邮件可选配置：

- `SMTP_HOST`
- `SMTP_PORT`
- `SMTP_USER`
- `SMTP_PASSWORD`
- `SMTP_FROM`

## Render 部署

现有 Render 服务名为 `pingdou`，继续连接 `xinyu250/pingdou` 仓库和 `main` 分支即可保留原有 `pingdou.onrender.com` 地址。

正式上线前必须在 Render Dashboard 配置长期 PostgreSQL 的 `DATABASE_URL`。免费 Web Service 的本地 SQLite 会在休眠、重启或重新部署后丢失；免费 Render Postgres 也只有 30 天有效期，不能作为正式数据源。

部署流程：

1. 创建或选择长期 PostgreSQL，并复制内部连接串。
2. 在现有 `pingdou` Web Service 设置上述环境变量。
3. 推送 `main`，等待 Render 自动部署。
4. 检查 `/api/health` 返回 `durable: true`。
5. 使用 `OWNER_EMAIL` 和 `OWNER_PASSWORD` 登录。
6. 在「账户设置 → 迁移旧版数据」上传部署前备份。
7. 验证库存、三张旧图纸、图纸图片和流水后再开放注册。

## 旧版数据迁移

部署前备份位于本地 `backups/preproduction-20260824.json`，该目录已加入 `.gitignore`，不会上传公开仓库。

管理员迁移接口只接收：

- 色号及库存数量
- 图纸名称、标签、来源、状态、图片和用豆明细

旧版 IP、访客 ID 和访问记录不会迁移。

## 生产验收标准

- `/api/health` 为 200 且 `durable: true`
- 未登录写请求返回 401，缺少 CSRF 的写请求返回 403
- 两个账号之间无法访问对方库存和图纸
- 数据库重启及应用重新部署后数据仍存在
- 注册、入库、出库、撤销、识图、保存图纸、记录进度、分享和导出均通过
- 390px 手机宽度下可以完成核心流程
- PostgreSQL 已开启平台备份并完成一次恢复演练
